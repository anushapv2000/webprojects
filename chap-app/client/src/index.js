import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'//everything we create is inside this app and
//putting it inside the root element inside the  html


// need to targe the root element from html
ReactDOM.render(<App/>,document.querySelector('#root'));

